[package]
name = "wasm-abi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Wire-format constants shared by the Edge Python compiler and the wasm-pdk. no_std, zero deps."

[lib]
crate-type = ["rlib"]
doctest = false
test = false
