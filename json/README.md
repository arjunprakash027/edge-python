# Edge Python JSON

JSON package for serialization, deserialization and manipulation. Scripts see `json` as ordinary.code-font

```python
from json import dumps, loads

deserialize = loads('{"hello": "world"}')
serialize = dumps(['foo', {'bar': ('baz', None, 1.0, 2)}])
```
